name: Automatic Sign In
on:
  schedule:
    - cron: '1/ 0,24 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: fish sign
       run: |
          python fishcsign.py ${secrets.FISHC_COOKIE}

